function vadd(a, b) {
    if (a.length !== b.length) {
      throw "Vectors have different lengths";
    }
    
    var result = [];
    for (var i = 0; i < a.length; i++) {
      result.push(a[i] + b[i]);
    }
    
    return result;
  }
